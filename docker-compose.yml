version: "3.9"

services:
  backend:
    build: ./peoplepost
    ports:
      - "5000:3000"
    env_file:
      - .env

  frontend:
    build: ./peoplepostF
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - backend
